USE tododb;

CREATE TABLE todos (
	id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    description TEXT,
    target_date DATETIME,
    due_date DATE,
    priority VARCHAR(20),
    status VARCHAR(20),
    tags VARCHAR(200),
    recurring VARCHAR(50)
);

